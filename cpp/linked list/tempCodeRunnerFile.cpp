curr = head;
    // Node *prev = NULL;
    // while(curr!=NULL){
    //     if(visited[curr]){
    //         Node* deleteNode = curr;
    //         prev->next = curr->next;
    //         curr = curr->next;
    //         delete(deleteNode);
    //     }
    //     else{
    //         prev = curr;
    //         curr = curr->next;
    //     }
    // }
    // return head;